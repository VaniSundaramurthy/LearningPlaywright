//Nested If Else examples real example in Javascript    

/* C++ code for nested if else example:
#include < iostream >
    using namespace std;
int main() {
    int age; */
cout << "Enter your age: ";
cin >> age;
if (age >= 18) {
    cout << "You are eligible to vote." << endl;
    if (age >= 21) {
        cout << "You are also eligible to drink alcohol." << endl;
    } else {
        cout << "You are not eligible to drink alcohol." << endl;
    }
} else {
    cout << "You are not eligible to vote." << endl;
    if (age >= 16) {
        cout << "You are eligible to drive." << endl;
    }
    else {
        cout << "You are not eligible to drive." << endl;
    }
}
return 0;
//}